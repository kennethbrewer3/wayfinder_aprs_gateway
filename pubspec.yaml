name: wayfinder_aprs_gateway
description: APRS KISS gateway that forwards parsed packets to a mapping server.
version: 1.0.0

environment:
  sdk: ^3.12.0

executables:
  wayfinder_aprs_gateway: wayfinder_aprs_gateway

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
